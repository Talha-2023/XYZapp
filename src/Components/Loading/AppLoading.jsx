import React from "react";
import styles from "./AppLoading.module.css";
const AppLoading = () => {
  return <div className={`${styles.app_container} ${styles.loading}`}></div>;
};

export default AppLoading;
